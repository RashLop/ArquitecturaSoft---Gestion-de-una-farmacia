@page
@model ProyectoArqSoft.Pages.Usuario.UsuarioCreateModel
@{
    ViewData["Title"] = "Nuevo Registro - VitalCare";
}

<div class="container py-5 mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">

            <div class="med-card shadow border-0">
                <div class="med-card-header text-center bg-success text-white py-3">
                    <h2 class="mb-0 fw-bold">Registro de Colaborador</h2>
                    <p class="mb-0 small">Las credenciales se generarán y enviarán automáticamente</p>
                </div>

                <div class="med-card-body p-4">
                    <form method="post" asp-page-handler="CrearUsuario" class="row g-3">

                        <!-- DATOS PERSONALES -->
                        <div class="col-md-12 form-floating mb-1">
                            <input asp-for="nombres" class="form-control med-input" placeholder="Nombre" required />
                            <label class="ms-2">Nombres</label>
                        </div>

                        <div class="col-md-6 form-floating">
                            <input asp-for="apPaterno" class="form-control med-input" placeholder="Primer Apellido" required />
                            <label class="ms-2">Primer Apellido</label>
                        </div>

                        <div class="col-md-6 form-floating">
                            <input asp-for="apMaterno" class="form-control med-input" placeholder="Segundo Apellido" />
                            <label class="ms-2">Segundo Apellido</label>
                        </div>

                        <!-- IDENTIFICACIÓN -->
                        <div class="col-md-4 form-floating">
                            <input asp-for="ci" class="form-control med-input" placeholder="CI" required />
                            <label class="ms-2">C.I.</label>
                        </div>

                        <div class="col-md-4 form-floating">
                            <select asp-for="ciExtencion" class="form-select med-input" required>
                                <option value="CB">CBBA</option>
                                <option value="LP">LA PAZ</option>
                                <option value="SC">SANTA CRUZ</option>
                                <option value="OR">ORURO</option>
                                <option value="PT">POTOSÍ</option>
                            </select>
                            <label class="ms-2">Extensión</label>
                        </div>

                        <div class="col-md-4 form-floating">
                            <input asp-for="telefono" class="form-control med-input" placeholder="Telf" required />
                            <label class="ms-2">Teléfono</label>
                        </div>

                        <!-- CONTACTO Y ROL -->
                        <div class="col-md-8 form-floating">
                            <input asp-for="email" type="email" class="form-control med-input" placeholder="Email" required />
                            <label class="ms-2">Correo Electrónico oficial</label>
                        </div>

                        <div class="col-md-4 form-floating">
                            <select asp-for="role" class="form-select med-input">
                                <option value="Bioquimico">Bioquímico (Personal)</option>
                                <option value="Admin">Administrador (Gestión)</option>
                            </select>
                            <label class="ms-2">Tipo de acceso</label>
                        </div>

                        <!-- MENSAJES DE ERROR -->
                        @if (!string.IsNullOrEmpty(Model.Estado.MensajeError))
                        {
                            <div class="alert alert-danger shadow-sm border-0 mt-3">
                                <i class="bi bi-exclamation-circle-fill me-2"></i> @Model.Estado.MensajeError
                            </div>
                        }

                        <!-- BOTONES -->
                        <div class="d-flex justify-content-end gap-2 mt-4 pt-3 border-top">
                            <a asp-page="Usuario" class="btn btn-secondary px-4">Cancelar</a>
                            <button type="submit" class="btn btn-success px-5 fw-bold shadow-sm" style="background-color: #198754;">
                                Generar y Registrar
                            </button>
                        </div>

                    </form>
                </div>
            </div>

        </div>
    </div>
</div>