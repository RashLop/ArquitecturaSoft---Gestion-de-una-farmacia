@page
@model ProyectoArqSoft.Pages.VentaUpdateModel
@{
}

<div class="container py-5 medicamento-form-page">
    <div class="row justify-content-center">
        <div class="col-lg-10">

            <div class="med-card shadow-sm">
                <div class="med-card-header text-center">
                    <h2 class="mb-0">Detalle de Venta</h2>
                    <p class="text-muted mt-2 mb-0">Consulta los datos de la venta registrada</p>
                </div>

                <div class="med-card-body">
                    <input type="hidden" asp-for="DetallesJson" id="DetallesJson" />
                    <input type="hidden" asp-for="IdCliente" id="IdCliente" />

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label fw-bold">Cliente (NIT)</label>
                            <input type="text"
                                   id="clienteBusqueda"
                                   class="form-control"
                                   readonly />
                        </div>

                        <div class="col-md-6 mb-3">
                            <label class="form-label fw-bold">Cliente</label>
                            <input type="text"
                                   id="razonSocialCliente"
                                   class="form-control"
                                   readonly />
                        </div>

                        <div class="col-md-6 mb-3">
                            <label asp-for="MetodoPago" class="form-label">M̩todo de Pago</label>
                            <input asp-for="MetodoPago" class="form-control" readonly />
                        </div>

                        <div class="col-md-6 mb-3">
                            <label class="form-label fw-bold">Id de Venta</label>
                            <input asp-for="IdVenta" class="form-control" readonly />
                        </div>
                    </div>

                    <hr />

                    <div class="mb-3">
                        <h5 class="mb-0">Detalle de venta</h5>
                    </div>

                    <div class="table-responsive">
                        <table class="table table-bordered align-middle" id="tablaDetalles">
                            <thead class="table-success">
                                <tr>
                                    <th>Medicamento</th>
                                    <th style="width:130px;">Cantidad</th>
                                    <th style="width:160px;">Precio Unitario</th>
                                    <th style="width:140px;">Subtotal</th>
                                </tr>
                            </thead>
                            <tbody id="detalleBody">
                            </tbody>
                        </table>
                    </div>

                    <div class="text-end mb-3">
                        <h5>Total: <span class="text-success" id="totalVenta">Bs. 0.00</span></h5>
                    </div>

                    @if (!string.IsNullOrWhiteSpace(Model.Estado.MensajeError))
                    {
                        <div class="alert alert-danger med-alert" role="alert">
                            @Model.Estado.MensajeError
                        </div>
                    }

                    <div class="d-flex justify-content-end gap-2 mt-4">
                        <a asp-page="Venta" class="btn med-btn-secondary">Volver</a>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>

<script>
    const clientes = [
        @foreach (System.Data.DataRow row in Model.ClienteDataTable.Rows)
        {
            <text>
            {
                id: @row["id"],
                nit: "@row["nit"]",
                razonSocial: "@row["razon_social"]"
            },
            </text>
        }
    ];

    const medicamentos = [
        @foreach (System.Data.DataRow row in Model.MedicamentoDataTable.Rows)
        {
            <text>
            {
                id: @row["id"],
                nombre: "@row["nombre"]"
            },
            </text>
        }
    ];

    const detalleBody = document.getElementById("detalleBody");
    const detallesJsonInput = document.getElementById("DetallesJson");
    const totalVentaSpan = document.getElementById("totalVenta");
    const clienteBusqueda = document.getElementById("clienteBusqueda");
    const razonSocialCliente = document.getElementById("razonSocialCliente");
    const idClienteInput = document.getElementById("IdCliente");

    function crearFila(detalle) {
        const tr = document.createElement("tr");
        const medicamento = medicamentos.find(m => Number(m.id) === Number(detalle?.IdMedicamento));
        const cantidad = Number(detalle?.Cantidad || 0);
        const precio = Number(detalle?.PrecioUnitario || 0);
        const subtotal = cantidad * precio;

        tr.innerHTML = `
            <td>${medicamento ? medicamento.nombre : "Medicamento no encontrado"}</td>
            <td>${cantidad}</td>
            <td>${precio.toFixed(2)}</td>
            <td>${subtotal.toFixed(2)}</td>
        `;

        detalleBody.appendChild(tr);
        return subtotal;
    }

    let detallesIniciales = [];
    try {
        detallesIniciales = JSON.parse(detallesJsonInput.value || "[]");
    } catch {
        detallesIniciales = [];
    }

    let total = 0;
    detallesIniciales.forEach(d => {
        total += crearFila(d);
    });
    totalVentaSpan.textContent = "Bs. " + total.toFixed(2);

    const clienteInicial = clientes.find(c => Number(c.id) === Number(idClienteInput.value));
    if (clienteInicial) {
        clienteBusqueda.value = clienteInicial.nit;
        razonSocialCliente.value = clienteInicial.razonSocial;
    }
</script>
