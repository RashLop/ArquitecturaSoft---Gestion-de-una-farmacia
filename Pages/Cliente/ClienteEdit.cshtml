@page "{id:int}"
@model ProyectoArqSoft.Pages.ClienteEditModel
@{
    ViewData["Title"] = "Editar Cliente";
}

<h1 class="mb-4">Editar Cliente</h1>

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="row">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header bg-warning text-dark">
                <h5 class="mb-0">Modifique los datos del cliente</h5>
            </div>
            <div class="card-body">
                <form method="post">
                    <input type="hidden" asp-for="IdCliente" />

                    <div class="form-group mb-3">
                        <label class="control-label">Tipo de Cliente</label>
                        <select asp-for="Tipo_Cliente" class="form-control" required>
                            <option value="">Seleccione...</option>
                            <option value="Regular">Regular</option>
                            <option value="Frecuente">Frecuente</option>
                            <option value="VIP">VIP</option>
                        </select>
                    </div>

                    <div class="form-group mb-3">
                        <label class="control-label">Nombre Completo</label>
                        <input asp-for="Nombre" class="form-control" required />
                    </div>

                    <div class="form-group mb-3">
                        <label class="control-label">Carnet de Identidad</label>
                        <input asp-for="Carnet" class="form-control" required />
                    </div>

                    <div class="form-group mb-3">
                        <label class="control-label">Edad</label>
                        <input asp-for="Edad" type="number" min="1" max="120" class="form-control" required />
                    </div>

                    <div class="form-group mb-3">
                        <label class="control-label">Teléfono</label>
                        <input asp-for="Telefono" class="form-control" required />
                    </div>

                    <div class="form-group">
                        <button type="submit" class="btn btn-warning">Guardar Cambios</button>
                        <a asp-page="Cliente" class="btn btn-secondary">Volver al Listado</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>